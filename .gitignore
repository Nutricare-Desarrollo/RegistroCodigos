# Dependencias
node_modules/
api/node_modules/

# Configuración local (nunca subir credenciales)
api/local.settings.json
*.env
.env

# Archivos temporales de build
_frontend_*.html

# Restos de herramientas (candados de git, pruebas) que no van al repositorio
_to_delete/
_tmp_probe_*

# Sistema
.DS_Store
Thumbs.db
